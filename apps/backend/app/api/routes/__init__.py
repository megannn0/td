"""Route modules for the Telegram Drive backend API.

This package collects all route modules for the backend. Each file in this
directory should define an APIRouter instance that encapsulates a set of
related endpoints. Route modules should avoid heavy logic and instead
delegate to service layer functions.
"""